This is an application to search for a specific file (can be typed in).
Works for Windows.
Starts an automatic recursive directories traversal in "C:\" directory using multiple threads.
For a file to be found its name should include its extension (e.g. example.txt).